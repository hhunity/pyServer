##

#watch dog
ファイル監視

#Flask 
python server

#Chart.js / Plotly.js で
html5でのグラフ描画

## erectron

#node.jsのインストール
brew install node
windowsはhttps://nodejs.org/

npm:node.jsのパッケージインストーラ
node.js:ブラウザ以外で JavaScript を実行できるエンジン
node hello.jsとすると、javaScriptが実行できる

#npm 初期化
npm init -y
→これでこれで package.json が生成される。

#electronをインストール
npm install --save-dev electron
※ --save-dev は開発用依存。
